# Internal / development-only (repo-root only; cluster ADRs live under skills/*/docs/)
/docs/
.cursor/

# Runtime outputs when using this repo as a workspace
projects/
# Legacy flat roots (deprecated write targets; may still exist locally)
research/
selection/
trees/
requirements/

# Local worked examples (product trees / shortlists — keep private, do not publish)
skills/*/examples/

# OS / editor
.DS_Store
Thumbs.db
*~
*.swp
*.swo
.idea/
.vscode/
*.code-workspace

# Local env / secrets
.env
.env.*
!.env.example
*.pem
credentials.json
